user-agent = "terminal/1.0"
referer = ";auto"

--ipv4

# Follow redirects
location

# Request compression
compressed

# When using -o, create the leading dirs if necessary
create-dirs

# FTP setup
ftp-create-dirs
ftp-ssl
ftp-pasv
ftp-method = nocwd

# Load a .netrc for login name and password if it exists
netrc-optional
